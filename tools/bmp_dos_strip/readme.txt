bmp_dos_strip takes a 16 colour bitmap (tested in MS Paint), flips XBGR to XRGB and outputs a bin file

- Written in Python 3.8.2
- Program input [bmp name] [output name]
- width and height must be divisable by 8 and the same 